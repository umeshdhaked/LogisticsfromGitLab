export interface VehicleManagement {
   id: number,
  vehicleNumber: string,
  driverName: string,
  vehicleType: string,
  capacity: number,
  costPerSlot: number,
  vehicleStatus: string,
  slot: string,
  date: string
}
